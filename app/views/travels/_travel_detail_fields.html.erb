<div class="nested-fields">
  <%= f.hidden_field :_destroy %>
  <p>
    <%= f.label :image %>
    <%= f.file_field :image, data: { image_url: f.object.image.url } %>
  </p>
  <p>
    <%= f.label :content %>
    <%= f.text_area :content %>
  </p>
  <%= link_to_remove_association "削除", f %>
</div>
